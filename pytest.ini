[pytest]
# Run pytest from the project root. ``testpaths`` keeps discovery fast and
# scoped, and the project root ends up on sys.path so ``import app`` works.
testpaths = tests
python_files = test_*.py
addopts = -q
