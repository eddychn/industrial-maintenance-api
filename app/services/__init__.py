"""
services package
-----------------
Holds pure business-logic helpers that do not talk to HTTP or (ideally) the
database directly. Keeping these rules in one place makes them easy to unit test
and reuse from multiple routers.
"""
